# Prompt for Codex

You are a world-class frontend engineer and UX designer. Your mission: transform an existing React app into a stunning, jaw-dropping interface.

## The App's Purpose

Read the code in this directory (should be named ./frontend) to fully understand what the app is doing.  Currently the app functions as desired so do not change the core functionality, but it needs a complete remake of the UI - it's terrible.

## Read the Skills First

> **CRITICAL:** You have the `frontend-design` skill use it.
> **CRITICAL:** You have the `uncodexify` skill — use it to prevent generic AI/Codex patterns.

## What Already Exists

- A terribly designed React frontend BUT with all the working route calls
- A backend that returns responses

## What You Must Build

- A complete rework of the frontend UI for this app
- Use the same Vite + React + Typescript that the app was already built with.

## Technical Notes

- Keep all existing backend wiring intact — don't break what works
- Structure the component architecture so adding new routes later requires minimal effort (the user will add more routes soon)
- Use Tailwind, Framer Motion, or whatever makes the animations sing
- Make it responsive but optimize for desktop (this is a power-user tool)

---

**The bar is high. Make it extraordinary.**